# 项目级执行规则

本文件适用于 `F:\四大指数及微盘股的择时选股策略探索` 及其子目录。
执行时必须先遵守 `C:\AGENTS.md`；本文件只补充本项目的研究、代码和交付约束。

## 项目范围

- 研究对象为中证500、中证1000、创业板指、科创50和中证2000的日频择时。
- 指数研究使用原始指数点位，不调用前复权语义，不把指数点位伪装成股票复权价格。
- 原始行情、供应商缓存、模型大文件和本地敏感凭证放在 `F:\data` 或用户私有目录，不提交到 Git。
- 项目内报告、脚本、配置、测试和小型审计产物可以提交；大体积二进制产物应先确认是否属于交付范围。

## 研究纪律

- 所有因子必须按可得时点处理；信号默认在 T 日收盘后形成，T+1 日执行。
- 标签、训练样本、滚动统计、缺失填补、截尾、标准化和分位点不得使用未来信息。
- 2015 年之前不使用宏观、情绪、资金和外部字段；缺少可靠发布时间的字段不得直接进入正式模型。
- 模型选择、因子选择、频率分组和仓位映射只能在开发期完成；已经观察过的历史 OOS 只能称为历史重放或验证，不得重新包装为全新样本外证据。
- 每个候选至少记录回测区间、有效交易日数、年化收益、基准收益、年化超额、策略夏普、超额夏普、最大回撤、换手、手续费、滑点和数据版本。
- 统一成本代理为单边手续费 3 bp、单边滑点 5 bp；成本按仓位变化计提，除非报告明确说明其他口径。
- 短历史指数必须报告样本长度和统计局限，不得与长历史指数的证据强度等同表述。

## 因子研究规范

- 因子来源、频率、可得性滞后、计算方法和经济含义必须可追溯。
- 因子按响应速度或预测期限分组时，应保留原始字段、分组规则和入选数量。
- 消融实验必须使用预先声明的输入组，至少区分快因子、慢因子和快慢组合；不能只报告最优配置。
- 因子贡献度应区分模型重要性、组级消融增益和策略绩效贡献，不能把单一指标解释成因果贡献。
- Alpha101 默认视为股票横截面因子库；迁移到指数时间序列择时时，必须说明改造后的输入、聚合方式和适用边界。

## 代码与文件

- 优先复用现有脚本、配置和数据合同；新增实验使用独立脚本和独立输出目录，不覆盖历史产物。
- 手工修改文件使用补丁方式；保持 ASCII 为默认编码，已有中文内容除外。
- 研究脚本应支持可重复运行，固定随机种子，并输出配置或 manifest。
- 报告生成脚本不得依赖未记录的人工筛选；HTML 报告须注明数据区间、研究状态和结果是否为历史重放。
- 修改前先检查 Git 工作区；不得撤销或覆盖用户已有改动。

## Git 与远端交付

- 默认分支为 `main`，远端名为 `origin`；提交信息使用简洁、可检索的英文或中英文混合描述。
- 提交时只加入本次任务明确产生的文件，不得把无关的用户改动、原始数据或本地缓存一起提交。
- 推送前必须检查 `git diff --cached`、提交状态和目标远端；推送后记录提交哈希和远端分支。
- 不使用交互式 Git 命令，不执行 `git reset --hard`、`git checkout --` 或未经确认的删除操作。
